#include <stdio.h>

/**
* main - entry point
* Description - prints lowercase alphabets
* Return: always 0
*/
int main(void)
{
	char ap = 'a';

	for (ap = 'a'; ap <= 'z'; ap++)
	{
		putchar (ap);
	}

	return (0);
}
