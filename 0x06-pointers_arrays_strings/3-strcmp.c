#include "main.h"
#include <stdio.h>

/**
 * _strcmp - compares two strings
 * @s1: string input
 * @s2: string input
 *
 * Return: returns 0, negative or postive int
 */

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
