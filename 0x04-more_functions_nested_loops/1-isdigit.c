#include "main.h"
#include <ctype.h>

/**
  * _isdigit - checks for uppercase character
  * @c: input character  that is checked
  *
  * Return: 1 if C is a digit otherwise 0
  */
int _isdigit(int c)
{
	if
	 isdigit(c)
	return (1);
	else
	return (0);


}
